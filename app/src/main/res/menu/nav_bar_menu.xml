<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/near_me_nav_bar"
        android:icon="@drawable/near_meh"
        android:title="Near Me"
        app:showAsAction="withText|always"/>

    <item android:id="@+id/dealofday_nav_bar"
        android:icon="@drawable/star_icon"
        android:title="Deal of the Day"
        app:showAsAction="withText|always"/>

    <item android:id="@+id/todaydeals_nav_bar"
        android:icon="@drawable/clock_icon"
        android:title="Today's Deals"
        app:showAsAction="withText|always"/>

    <item android:id="@+id/planahead_nav_bar"
        android:icon="@drawable/calendar_icon"
        android:title="Plan Ahead"
        app:showAsAction="withText|always"/>
</menu>